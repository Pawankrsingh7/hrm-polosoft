PORT=10000

# Preferred on Render: paste the full connection string from your PostgreSQL service.
# Example: postgresql://user:password@host:5432/database
DATABASE_URL=

# Optional fallback if DATABASE_URL is not set
PGHOST=
PGPORT=5432
PGDATABASE=
PGUSER=
PGPASSWORD=

# In production Render uses SSL for PostgreSQL
PGSSL=true

# Admin login credentials for /admin/login
ADMIN_USERNAME=admin
ADMIN_PASSWORD=admin@108
ADMIN_SESSION_SECRET=change-this-secret

# SMTP (nodemailer) for employee notifications
SMTP_HOST=
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
SMTP_FROM_NAME=HR Team
SMTP_FROM_EMAIL=
